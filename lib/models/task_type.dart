enum PrayerStatus {
  notPerformed,
  performed,
  late,
}

enum TaskType {
  fard,     // Mandatory (5 prayers)
  sunnah,   // Sunnah (Qiyam al-Layl)
}

// TaskType extension methods removed as they were unused

